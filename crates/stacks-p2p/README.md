# stacks-p2p

P2P protocol versioning and peer-facing types for the Stacks blockchain.

This crate is currently a small boundary marker for P2P-specific concepts that
should not be owned by epoch schedules or general protocol policy.
