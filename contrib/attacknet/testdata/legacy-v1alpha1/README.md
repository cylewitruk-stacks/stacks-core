# Legacy v1alpha1 fixtures

These JSON documents are machine fixtures for compatibility tests of the
retiring JavaScript and shell command surface. They are not human examples.
New resources belong in `contrib/attacknet/examples/` as v1beta1 YAML.
