# Release 1 Amendment A7 evidence

A7 retires the executable v1alpha1 JavaScript and shell runtime after the Go
controller migration. Its Reduced-tier packet must bind the exact signed A6
child, every deleted path, immutable equivalence fixtures, complete product
checks, and a portable evidence archive.

The runtime and Kubernetes API are unchanged. Historical packets remain bound
to the revisions that contained their original source paths.
