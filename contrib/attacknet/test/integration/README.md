# Attacknet integration tests

Place cross-subsystem integration tests here when they do not belong to one
operator or product subsystem. The offline entry point remains
[`../check.sh`](../check.sh).
