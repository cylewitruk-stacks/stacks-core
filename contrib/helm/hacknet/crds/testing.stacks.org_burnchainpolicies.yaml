---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: burnchainpolicies.testing.stacks.org
spec:
  group: testing.stacks.org
  names:
    kind: BurnchainPolicy
    listKind: BurnchainPolicyList
    plural: burnchainpolicies
    singular: burnchainpolicy
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: BurnchainPolicy declares externally controlled Bitcoin regtest
          mining policy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BurnchainPolicySpec defines bootstrap and steady-state block
              production.
            properties:
              bitcoinNodeRef:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              bootstrapHeight:
                format: int64
                maximum: 10000000
                minimum: 0
                type: integer
              cadence:
                type: string
              destinationSelection:
                default: round-robin
                description: BurnchainDestinationMode controls how the clock chooses
                  a destination.
                enum:
                - round-robin
                - fixed
                type: string
              destinations:
                items:
                  description: BurnchainDestinationSpec binds one watch-only wallet
                    to a mining address.
                  properties:
                    address:
                      maxLength: 128
                      minLength: 1
                      pattern: ^[^,\r\n]+$
                      type: string
                    walletName:
                      maxLength: 128
                      minLength: 1
                      pattern: ^[^,\r\n]+$
                      type: string
                  required:
                  - address
                  - walletName
                  type: object
                maxItems: 64
                minItems: 1
                type: array
              fixedDestinationIndex:
                format: int32
                maximum: 63
                minimum: 0
                type: integer
              flash:
                description: BurnchainFlashRequest is one idempotent bounded multi-block
                  request.
                properties:
                  blocks:
                    format: int32
                    maximum: 10000
                    minimum: 1
                    type: integer
                  id:
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  interval:
                    type: string
                required:
                - blocks
                - id
                type: object
              networkRef:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              paused:
                type: boolean
              rpc:
                description: BurnchainRPCSpec configures bounded RPC retry behavior.
                properties:
                  maximumBackoff:
                    type: string
                  minimumBackoff:
                    type: string
                  passwordSecretRef:
                    description: SecretKeyReference identifies one key in a Secret.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  timeout:
                    type: string
                  usernameSecretRef:
                    description: SecretKeyReference identifies one key in a Secret.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
                x-kubernetes-validations:
                - message: usernameSecretRef and passwordSecretRef must be configured
                    together
                  rule: has(self.usernameSecretRef) == has(self.passwordSecretRef)
            required:
            - bitcoinNodeRef
            - cadence
            - destinations
            - networkRef
            type: object
            x-kubernetes-validations:
            - message: fixedDestinationIndex must select a destination only in fixed
                mode
              rule: 'self.destinationSelection == ''fixed'' ? (!has(self.fixedDestinationIndex)
                || self.fixedDestinationIndex < size(self.destinations)) : (!has(self.fixedDestinationIndex)
                || self.fixedDestinationIndex == 0)'
          status:
            description: BurnchainPolicyStatus reports applied policy and observed
              Bitcoin progress.
            properties:
              admittedBitcoinRuntimeImageID:
                type: string
              admittedBitcoinStatefulSetUID:
                type: string
              admittedNetworkUID:
                type: string
              appliedFlashId:
                type: string
              appliedPolicyDigest:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consecutiveFailures:
                format: int32
                type: integer
              lastAttemptAt:
                format: date-time
                type: string
              lastBlockHash:
                type: string
              lastSuccessAt:
                format: date-time
                type: string
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              observedHeight:
                format: int64
                type: integer
              phase:
                type: string
              reason:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
