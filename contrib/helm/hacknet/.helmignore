.DS_Store
.git/
.github/
# Source, examples, and developer scripts are not chart payload. Keeping the
# Go module in Helm release metadata can exceed Kubernetes' 1 MiB Secret limit.
operator/
examples/
scripts/
